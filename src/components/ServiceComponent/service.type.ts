export type ServiceComponentProps = {
  // icon: JSX.Element;
  heading: string;
  children: JSX.Element;
  // active?: boolean;
};
export type ServiceElementComponentProps = {
  // icon: JSX.Element;
  elementHeading: string;
  elementText: string;
  image: string;
  // active?: boolean;
};
